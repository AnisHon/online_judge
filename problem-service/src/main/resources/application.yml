server:
  port: 8081
spring:
  config:
    name: problem-service
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 200MB


oj:
  db:
    database: db_problem

  knife4j:
    resource: com.anishan.problem

  minio:
    bucket-name: case