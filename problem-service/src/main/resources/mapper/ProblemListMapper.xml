<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anishan.problem.mapper.ProblemListMapper">

    <resultMap id="BaseResultMap" type="com.anishan.problem.domain.entity.ProblemList">
            <id property="listId" column="list_id" jdbcType="BIGINT"/>
            <result property="listName" column="list_name" jdbcType="VARCHAR"/>
            <result property="description" column="description" jdbcType="VARCHAR"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
            <result property="delFlag" column="del_flag" jdbcType="TINYINT"/>
    </resultMap>
    <select id="selectContestProblemByListId" resultType="com.anishan.problem.domain.vo.ProblemInListVo">
        select
            ppl.score as score,
            ppl.problem_order as problemOrder,
            ppl.problem_id as problemId,
            p.title as title,
            p.type as type,
            p.source as source,
            p.description as description,
            p.hint as hint,
            p.auth as auth,
            p.create_time as createTime,
            cr.score as userScore,
            cr.status as correct
        from contest c
        left join problem_problem_list ppl on c.list_id = ppl.list_id
        left join problem p on p.problem_id = ppl.problem_id
            and p.del_flag = 0
        left join contest_records cr on cr.problem_id = ppl.problem_id
            and cr.contest_id = #{contestId}
            and cr.user_id = #{userId}

        where
            not isnull(p.problem_id)
        and
            ppl.list_id = #{listId}
        <if test="contestId != null">
            and c.contest_id = #{contestId}
        </if>
    </select>
    <select id="selectProblemByListId" resultType="com.anishan.problem.domain.vo.ProblemInListVo">
        select
            ppl.score as score,
            ppl.problem_order as problemOrder,
            ppl.problem_id as problemId,
            p.title as title,
            p.type as type,
            p.source as source,
            p.description as description,
            p.hint as hint,
            p.auth as auth,
            p.create_time as createTime,
            exists(select 1 from problem_complete pc where pc.problem_id = ppl.problem_id and pc.user_id = #{userId}) as finish
        from problem_problem_list ppl
        left join problem p on p.problem_id = ppl.problem_id

        where
            not isnull(p.problem_id)
        and
            ppl.list_id = #{listId}
    </select>


</mapper>
