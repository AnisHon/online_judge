spring:
  web:
    resources:
      add-mappings: false

  mvc:
    throw-exception-if-no-handler-found: true

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8 # +8 北京时间

feign:
  okhttp:
    enabled: true
  httpclient:
    enabled: false

oj:
  product: false