server:
  port: 8082
spring:
  application:
    name: judge-server # 服务名称
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-jdbc.yaml  # 共享mybatis配置
          - data-id: shared-logging.yaml
          - data-id: shared-redis.yaml
          - data-id: shared-common.yaml
          - data-id: shared-rabbit.yaml



